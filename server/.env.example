# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: OpenAI Project ID (for organization/project scoping)
# Get your Project ID from: https://platform.openai.com/settings/organization
OPENAI_PROJECT_ID=
OPENAI_MODEL_CHAT=gpt-5.1-mini

# Server Configuration
PORT=4000

# Optional: CORS Configuration (for production, specify allowed origins)
# CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# Optional: Local Network IP (for mobile device access)
# If not set, server will attempt to auto-detect from network interfaces
# LOCAL_NETWORK_IP=192.168.1.52
